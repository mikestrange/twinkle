package org.web.sdk.utils 
{
	public class CofFix 
	{
		public static function parse(data:String) : void 
		{
			/*
			data = data.replace(/\r\n/g, "\n");
			data = data.replace(/\r/g, "\n");
			var arr:Array = data.split(/\n/);
			var length:int = arr.length;
			var i:int = 0;
			while (i < length) {
				var str:String = arr;
				if (str.length > 0) 
				{
					//str = StringUtil.trim(str);
					var index:int = str.search(/\[[A-Z0-9_]+\]/);
					if (index == 0)
					{
						//[stageSize]标签行
					}else{
						//width=800值行
					}
			}*/
		}
			
		//ends
	}

}